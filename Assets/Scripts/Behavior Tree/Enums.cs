public class Enums {
    public enum states {
        success,
        failure,
        running
    };

    public enum types {
        decorator,
        selector,
        sequence,
        action
    }

}
